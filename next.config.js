/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "convex.dev",
      "next-mallard-647.convex.cloud",
      "confident-cobra-406.convex.cloud",
    ],
  },
};

export default nextConfig;
